# MindCode Design System v1.0
## AI-Native IDE 设计系统规范

---

## A) 总体信息架构与交互模型

### AI Panel 顶栏结构

```
┌─────────────────────────────────────────────────────────┐
│ [AI Icon] MindCode AI  │ [Mode] [Model] [Context] [Pin] │
└─────────────────────────────────────────────────────────┘
│ [Chat] [Plan] [Agent] [Debug] ← Segmented Control      │
├─────────────────────────────────────────────────────────┤
│ [Context Chips Area]                                    │
└─────────────────────────────────────────────────────────┘
```

**顶栏组件（从左到右）：**
1. **AI 标识图标**：28×28px，渐变背景（紫→蓝），圆角 6px
2. **标题**："MindCode AI" 或当前会话名称（可编辑）
3. **模式切换器**：Segmented Control，4 个选项
4. **模型选择器**：下拉菜单，宽度 140px
5. **上下文入口**：图标按钮，显示已添加上下文数量（badge）
6. **历史会话**：图标按钮，下拉面板
7. **Pin 按钮**：固定面板（防止自动折叠）

### 模式切换规则

| 从模式 | 到模式 | 上下文处理 | 用户提示 |
|--------|--------|------------|----------|
| Chat | Plan | 保留（作为 Plan 输入） | 无 |
| Chat | Agent | 清空（Agent 需新 Plan） | 提示："需要先创建 Plan" |
| Chat | Debug | 保留（错误信息作为上下文） | 无 |
| Plan | Chat | 保留 Plan 内容 | 无 |
| Plan | Agent | 自动加载 Plan | 提示："准备执行 Plan" |
| Plan | Debug | 清空 | 无 |
| Agent | Chat | 保留执行历史 | 无 |
| Agent | Plan | 保留执行历史 | 无 |
| Agent | Debug | 保留执行历史 | 无 |
| Debug | Chat | 保留诊断信息 | 无 |
| Debug | Plan | 保留诊断信息 | 无 |
| Debug | Agent | 需要先有 Plan | 提示："需要先创建 Plan" |

### "低打扰"策略

1. **默认宽度**：360px（可拖拽调整 280px - 600px）
2. **默认状态**：折叠（仅显示 48px 宽度的侧边栏图标）
3. **自动展开条件**：
   - AI 响应完成时
   - 用户主动点击侧边栏图标
   - 快捷键触发（Ctrl+L / Cmd+L）
   - Agent 执行中
   - Debug 检测到错误
4. **临时浮层**：快速操作（如应用 diff）使用临时浮层，不占用面板空间
5. **自动折叠条件**：
   - 用户点击编辑器区域
   - 30 秒无交互（可配置）
   - 面板宽度 < 280px 时自动折叠

### 与 Editor 的联动

1. **引用选区**：
   - 用户选中代码 → 右键 "Add to AI Context" 或快捷键
   - 在 AI Panel 显示为 Context Chip
   - 编辑器选区高亮（淡蓝色，2px 边框）
2. **应用 diff**：
   - Agent/Debug 生成的 diff → 点击 "Apply" → 编辑器显示变更预览
   - 支持逐个文件应用或批量应用
3. **跳转到行**：
   - 点击消息中的文件路径 → 编辑器跳转并高亮
4. **打开文件**：
   - 点击文件引用 → 在编辑器打开新标签页

---

## B) 设计 Tokens

### 1. 颜色系统

#### 背景层级（深色主题）
```css
--bg-0: #0a0a0c;        /* 最深层：编辑器背景 */
--bg-1: #0f0f12;        /* 面板背景：侧边栏、AI Panel */
--bg-2: #151519;        /* 浮层背景：下拉菜单、对话框 */
--bg-3: #1a1a1e;        /* 高亮背景：hover、选中态 */
--bg-overlay: rgba(0, 0, 0, 0.75);  /* 遮罩层 */
```

#### 面板与容器
```css
--panel-bg: var(--bg-1);
--panel-border: rgba(255, 255, 255, 0.04);
--panel-elevated: var(--bg-2);
```

#### 边框
```css
--border-subtle: rgba(255, 255, 255, 0.04);   /* 几乎不可见：分割线 */
--border-default: rgba(255, 255, 255, 0.06);  /* 默认边框：输入框、卡片 */
--border-strong: rgba(255, 255, 255, 0.1);    /* 强调边框：聚焦态 */
--border-focus: #3b82f6;                      /* 聚焦边框：蓝色 */
```

#### 文本层级
```css
--text-primary: #f5f5f7;      /* 主要文本：标题、正文 */
--text-secondary: #a1a1aa;    /* 次要文本：描述、辅助信息 */
--text-muted: #71717a;        /* 静音文本：时间戳、标签 */
--text-disabled: #52525b;     /* 禁用文本 */
```

#### 强调色
```css
--accent-primary: #3b82f6;    /* 主强调：按钮、链接、聚焦 */
--accent-secondary: #60a5fa;  /* 次强调：hover 态 */
--accent-ai: linear-gradient(135deg, #8b5cf6 0%, #3b82f6 100%);  /* AI 标识渐变 */
```

#### 语义色
```css
--semantic-success: #22c55e;
--semantic-warning: #f59e0b;
--semantic-error: #ef4444;
--semantic-info: #3b82f6;

--semantic-success-bg: rgba(34, 197, 94, 0.08);
--semantic-warning-bg: rgba(245, 158, 11, 0.08);
--semantic-error-bg: rgba(239, 68, 68, 0.08);
--semantic-info-bg: rgba(59, 130, 246, 0.08);
```

### 2. 字体系统

#### UI 字体
```css
--font-ui: "Inter", -apple-system, BlinkMacSystemFont, sans-serif;
--font-ui-weight-normal: 400;
--font-ui-weight-medium: 500;
--font-ui-weight-semibold: 600;
```

#### 代码字体
```css
--font-code: "JetBrains Mono", "Fira Code", "SF Mono", Consolas, monospace;
--font-code-weight: 400;
```

#### 字号阶梯
```css
--font-size-xs: 11px;    /* 标签、时间戳 */
--font-size-sm: 12px;    /* 辅助文本、按钮 */
--font-size-base: 13px;  /* 正文、输入框 */
--font-size-md: 14px;    /* 标题、强调文本 */
--font-size-lg: 16px;    /* 大标题 */
```

#### 行高
```css
--line-height-tight: 1.25;    /* 标题 */
--line-height-normal: 1.5;   /* 正文 */
--line-height-relaxed: 1.65;  /* 长文本、代码块 */
--line-height-code: 1.6;      /* 代码 */
```

### 3. 间距与密度

#### Spacing Scale（4px 基准）
```css
--space-0: 0;
--space-1: 4px;
--space-2: 8px;
--space-3: 12px;
--space-4: 16px;
--space-5: 20px;
--space-6: 24px;
--space-8: 32px;
--space-10: 40px;
--space-12: 48px;
```

#### 组件 Padding 规范
```css
/* 按钮 */
--btn-padding-sm: 6px 12px;    /* 小按钮 */
--btn-padding-md: 8px 16px;    /* 中按钮 */
--btn-padding-lg: 10px 20px;   /* 大按钮 */

/* 输入框 */
--input-padding: 8px 12px;
--input-height: 32px;

/* 卡片 */
--card-padding: 16px;
--card-padding-lg: 20px;

/* 面板内边距 */
--panel-padding: 16px;
--panel-padding-sm: 12px;
```

### 4. 圆角/阴影/描边

#### 圆角
```css
--radius-xs: 2px;    /* 标签、badge */
--radius-sm: 4px;    /* 按钮、输入框 */
--radius-md: 6px;    /* 卡片、下拉菜单 */
--radius-lg: 8px;    /* 大卡片、对话框 */
--radius-xl: 12px;   /* 特殊容器 */
--radius-full: 9999px;  /* 圆形 */
```

#### 阴影
```css
--shadow-xs: 0 1px 2px rgba(0, 0, 0, 0.4);
--shadow-sm: 0 2px 4px rgba(0, 0, 0, 0.4);
--shadow-md: 0 4px 12px rgba(0, 0, 0, 0.5);
--shadow-lg: 0 8px 24px rgba(0, 0, 0, 0.6);
--shadow-focus: 0 0 0 2px rgba(59, 130, 246, 0.3);
```

#### 描边
```css
--border-width-thin: 1px;
--border-width-medium: 1.5px;
--border-width-thick: 2px;
```

---

## C) AI Panel 四模式组件规范

### 1. 顶栏：模式 Segmented Control

**结构：**
```
┌──────┬──────┬──────┬──────┐
│ Chat │ Plan │Agent │Debug │
└──────┴──────┴──────┴──────┘
```

**尺寸：**
- 总宽度：自适应（每个选项 80-100px）
- 高度：32px
- 圆角：6px
- 选项间距：2px（内部）

**状态：**
- **默认**：背景 `--bg-2`，文字 `--text-secondary`
- **选中**：背景 `--bg-3`，文字 `--text-primary`，左侧 2px 蓝色指示条
- **Hover**：背景 `--bg-2`，文字 `--text-primary`
- **Disabled**：透明度 0.4，不可点击
- **Badge**：Debug 模式右上角红点（6×6px），表示有错误

**交互：**
- 点击切换模式，160ms 过渡动画
- 切换时显示加载状态（可选）

### 2. Context Chips（上下文标签）

**类型与视觉区分：**

| 类型 | 图标 | 背景色 | 边框色 | 文字色 |
|------|------|--------|--------|--------|
| 文件 | 📄 | `rgba(59, 130, 246, 0.08)` | `rgba(59, 130, 246, 0.2)` | `--text-primary` |
| 选区 | ✂️ | `rgba(139, 92, 246, 0.08)` | `rgba(139, 92, 246, 0.2)` | `--text-primary` |
| 错误日志 | ⚠️ | `rgba(239, 68, 68, 0.08)` | `rgba(239, 68, 68, 0.2)` | `--text-primary` |
| 终端输出 | 💻 | `rgba(34, 197, 94, 0.08)` | `rgba(34, 197, 94, 0.2)` | `--text-primary` |
| Git Diff | 🔀 | `rgba(245, 158, 11, 0.08)` | `rgba(245, 158, 11, 0.2)` | `--text-primary` |

**尺寸：**
- 高度：24px
- 内边距：4px 8px
- 圆角：4px
- 图标：14×14px
- 移除按钮：16×16px（hover 显示）

**交互：**
- 点击：跳转到对应文件/位置
- 移除：hover 显示 × 按钮，点击移除
- 锁定：右键菜单 "Lock Context"（锁定后不可移除）

### 3. Chat 视图

**消息结构：**

```
┌─────────────────────────────────────┐
│ [Avatar] Role Name        [Time]    │
│         Message content...          │
│         ┌─────────────────┐        │
│         │ Code Block      │        │
│         │ [Copy] [Insert] │        │
│         └─────────────────┘        │
└─────────────────────────────────────┘
```

**推荐方案：平铺消息块（非气泡）**

**理由：**
- 更符合 IDE 工具属性（信息密度高）
- 减少视觉噪音（无圆角、无背景色块）
- 代码块更易阅读（无嵌套背景）
- 与编辑器风格一致

**组件尺寸：**
- 消息间距：20px
- Avatar：26×26px，圆角 6px
- 消息内容：左距 avatar 36px
- 代码块工具条：高度 32px，背景 `--bg-2`

**状态：**
- **用户消息**：左对齐，avatar 灰色背景
- **AI 消息**：左对齐，avatar 渐变背景
- **流式输出**：显示光标动画
- **错误消息**：红色边框，错误图标

### 4. Plan 视图

**Plan Card 结构：**

```
┌─────────────────────────────────────┐
│ Plan: [Title]              [v1] [v2]│
│ ─────────────────────────────────── │
│ 🎯 Goal: [描述]                     │
│ 📋 Assumptions:                     │
│   • Assumption 1                    │
│   • Assumption 2                    │
│ ─────────────────────────────────── │
│ 📍 Milestones:                      │
│   [✓] M1: Setup (2h)               │
│   [ ] M2: Implement (4h)            │
│ ─────────────────────────────────── │
│ ✅ Tasks:                           │
│   [ ] Task 1                        │
│   [ ] Task 2                        │
│ ─────────────────────────────────── │
│ ⚠️ Risks:                           │
│   • Risk 1                          │
│ ─────────────────────────────────── │
│ [Edit] [Lock Plan] [Execute]        │
└─────────────────────────────────────┘
```

**尺寸：**
- 卡片宽度：100%
- 卡片内边距：20px
- 章节间距：16px
- 任务项高度：28px
- 复选框：16×16px

**交互：**
- **编辑**：点击 "Edit" → 进入编辑模式（可修改文本）
- **确认任务**：勾选复选框 → 任务标记为完成
- **锁定计划**：点击 "Lock Plan" → 计划不可编辑，可执行
- **版本对比**：点击 v1/v2 → 显示 diff 视图

### 5. Agent 视图

**Stepper 结构：**

```
┌─────────────────────────────────────┐
│ Executing Plan: [Plan Name]         │
│ ─────────────────────────────────── │
│ ● Step 1: Read file.ts      [Done]  │
│ ● Step 2: Modify file.ts    [Done]  │
│ ● Step 3: Create new.ts     [Running]│
│ ○ Step 4: Run command       [Queued]│
│ ─────────────────────────────────── │
│ Changes Preview:                     │
│ 📄 file.ts (2 changes)      [Apply]  │
│ 📄 new.ts (new file)        [Apply]  │
│ ─────────────────────────────────── │
│ [Cancel] [Apply All] [Rollback]     │
└─────────────────────────────────────┘
```

**状态图标：**
- **Queued**：○ 灰色圆圈
- **Running**：⟳ 蓝色旋转图标
- **Succeeded**：✓ 绿色对勾
- **Failed**：✗ 红色叉号
- **Cancelled**：⊘ 灰色取消图标

**变更预览：**
- 文件列表：文件名 + 变更数量
- 点击文件名 → 展开 diff 视图
- 每个文件有独立 "Apply" 按钮
- "Apply All" 批量应用

**尺寸：**
- Step 高度：40px
- Step 间距：8px
- 变更项高度：36px
- 按钮高度：32px

### 6. Debug 视图

**结构：**

```
┌─────────────────────────────────────┐
│ 🐛 Issue: [Error Message]           │
│ ─────────────────────────────────── │
│ 📊 Observations:                    │
│   • Observation 1                   │
│   • Observation 2                   │
│ ─────────────────────────────────── │
│ 💡 Hypotheses:                      │
│   [H1] Hypothesis 1                 │
│   [H2] Hypothesis 2                 │
│ ─────────────────────────────────── │
│ 🔍 Verification Steps:              │
│   [ ] Step 1                        │
│   [ ] Step 2                        │
│ ─────────────────────────────────── │
│ 🔧 Fix Options:                     │
│   Option 1: [Preview] [Apply]       │
│   Option 2: [Preview] [Apply]       │
│ ─────────────────────────────────── │
│ [View Logs] [View Stack]            │
│ [Re-run Test] [Re-execute]          │
└─────────────────────────────────────┘
```

**日志/堆栈展示：**
- 默认折叠（显示前 3 行）
- 点击展开 → 显示完整内容
- 可复制、可搜索

**修复补丁：**
- 以 diff 形式展示（绿色新增、红色删除）
- "Apply" 按钮 → 应用到工作区
- "Copy" 按钮 → 复制 diff 文本
- "Preview" 按钮 → 在编辑器预览

**尺寸：**
- Issue 卡片：内边距 20px
- 章节间距：16px
- 修复选项卡片：内边距 16px
- 按钮高度：32px

### 7. 通用状态组件

**Empty State：**
```
┌─────────────────────────────────────┐
│         [Icon 48×48px]              │
│         No context yet              │
│         Add files or selection      │
│         [Add Context Button]        │
└─────────────────────────────────────┘
```
- 图标：灰色，透明度 0.4
- 文字：`--text-muted`
- 按钮：主按钮样式

**Loading：**
- 使用 3 个点的动画（6×6px，间距 4px）
- 颜色：`--accent-primary`
- 位置：消息区域居中

**Error：**
```
┌─────────────────────────────────────┐
│ ⚠️ [Error Title]                     │
│ [Error Message]                     │
│ [Retry] [Dismiss]                   │
└─────────────────────────────────────┘
```
- 背景：`--semantic-error-bg`
- 边框：`--semantic-error`，1px
- 图标：20×20px

**Confirm（高风险操作）：**
- 使用模态对话框
- 背景：`--bg-overlay`
- 对话框：`--bg-2`，圆角 12px，阴影 `--shadow-lg`
- 按钮：危险操作使用红色按钮

---

## D) 动效与质感细节

### 面板切换动效
- **时长**：180ms
- **Easing**：`cubic-bezier(0.4, 0, 0.2, 1)`
- **效果**：淡入 + 轻微缩放（scale: 0.98 → 1.0）

### 列表展开
- **时长**：200ms
- **Easing**：`ease-out`
- **效果**：高度过渡 + 透明度

### Diff 展示
- **时长**：150ms
- **效果**：淡入，无缩放

### Chip 添加/移除
- **添加**：从左侧滑入（translateX: -20px → 0），200ms
- **移除**：淡出 + 缩放（scale: 1 → 0.8），150ms

### 禁止过度动画
- ❌ 页面切换时的旋转/翻转
- ❌ 按钮点击时的弹跳
- ❌ 消息出现时的弹跳
- ❌ 过长的加载动画（> 2s）
- ❌ 背景粒子和闪烁效果

---

## E) 最终输出

### 1. AI Panel 组件树

```
AI Panel
├── Header
│   ├── AI Icon
│   ├── Title (editable)
│   ├── Model Selector
│   ├── Context Button (with badge)
│   ├── History Button
│   └── Pin Button
├── Mode Switcher (Segmented Control)
│   ├── Chat Tab
│   ├── Plan Tab
│   ├── Agent Tab
│   └── Debug Tab (with error badge)
├── Context Chips Area
│   └── Context Chip (multiple)
│       ├── Icon
│       ├── Label
│       └── Remove Button
└── Content Area
    ├── Chat View
    │   ├── Message List
    │   │   └── Message Item
    │   │       ├── Avatar
    │   │       ├── Header (role + time)
    │   │       ├── Body
    │   │       ├── Code Block (optional)
    │   │       │   └── Toolbar (Copy/Insert/Apply)
    │   │       └── Actions (optional)
    │   ├── Typing Indicator
    │   └── Empty State
    ├── Plan View
    │   ├── Plan Card
    │   │   ├── Header (title + version)
    │   │   ├── Goal Section
    │   │   ├── Assumptions Section
    │   │   ├── Milestones Section
    │   │   ├── Tasks Section
    │   │   ├── Risks Section
    │   │   └── Actions (Edit/Lock/Execute)
    │   └── Version Diff (optional)
    ├── Agent View
    │   ├── Stepper
    │   │   └── Step Item
    │   │       ├── Status Icon
    │   │       ├── Label
    │   │       └── Status Badge
    │   ├── Changes Preview
    │   │   └── File Change Item
    │   │       ├── File Name
    │   │       ├── Change Count
    │   │       ├── Diff Preview (expandable)
    │   │       └── Apply Button
    │   └── Actions (Cancel/Apply All/Rollback)
    └── Debug View
        ├── Issue Card
        │   ├── Title
        │   └── Description
        ├── Observations Section
        ├── Hypotheses Section
        ├── Verification Steps Section
        ├── Fix Options Section
        │   └── Fix Option Card
        │       ├── Description
        │       ├── Diff Preview
        │       └── Actions (Preview/Apply/Copy)
        ├── Logs Viewer (collapsible)
        ├── Stack Viewer (collapsible)
        └── Actions (Re-run/Re-execute)
```

### 2. 四模式关键页面线框

#### Chat 模式
```
┌─────────────────────────────────────┐
│ [Header with mode switcher]         │
├─────────────────────────────────────┤
│ [Context Chips]                     │
├─────────────────────────────────────┤
│                                     │
│ [Avatar] User                       │
│         Message content...          │
│                                     │
│ [Avatar] AI                         │
│         Response content...         │
│         ┌─────────────────┐        │
│         │ Code Block      │        │
│         │ [Copy][Insert]  │        │
│         └─────────────────┘        │
│                                     │
│ [Typing Indicator...]               │
│                                     │
├─────────────────────────────────────┤
│ [Input Area]                        │
│ [Send Button]                       │
└─────────────────────────────────────┘
```

#### Plan 模式
```
┌─────────────────────────────────────┐
│ [Header]                            │
├─────────────────────────────────────┤
│ ┌─────────────────────────────────┐ │
│ │ Plan: Feature Implementation   │ │
│ │ ─────────────────────────────── │ │
│ │ 🎯 Goal: ...                    │ │
│ │ 📋 Assumptions: ...              │ │
│ │ 📍 Milestones:                  │ │
│ │   [✓] M1: Setup                 │ │
│ │   [ ] M2: Implement             │ │
│ │ ✅ Tasks:                       │ │
│ │   [ ] Task 1                    │ │
│ │   [ ] Task 2                    │ │
│ │ ⚠️ Risks: ...                    │ │
│ │ [Edit] [Lock] [Execute]         │ │
│ └─────────────────────────────────┘ │
└─────────────────────────────────────┘
```

#### Agent 模式
```
┌─────────────────────────────────────┐
│ [Header]                            │
├─────────────────────────────────────┤
│ Executing: Feature Plan             │
│ ─────────────────────────────────── │
│ ● Step 1: Read file.ts      [Done]  │
│ ● Step 2: Modify file.ts    [Done]  │
│ ● Step 3: Create new.ts     [Running]│
│ ○ Step 4: Run test          [Queued]│
│ ─────────────────────────────────── │
│ Changes:                            │
│ 📄 file.ts (+15, -3)        [Apply] │
│ 📄 new.ts (new)              [Apply] │
│ ─────────────────────────────────── │
│ [Cancel] [Apply All] [Rollback]     │
└─────────────────────────────────────┘
```

#### Debug 模式
```
┌─────────────────────────────────────┐
│ [Header]                            │
├─────────────────────────────────────┤
│ 🐛 TypeError: Cannot read property  │
│ ─────────────────────────────────── │
│ 📊 Observations:                    │
│   • Error in line 42                │
│   • Variable 'x' is undefined       │
│ ─────────────────────────────────── │
│ 💡 Hypotheses:                      │
│   [H1] Missing null check           │
│   [H2] Variable not initialized     │
│ ─────────────────────────────────── │
│ 🔧 Fix Options:                     │
│   Option 1: Add null check          │
│   [Preview Diff] [Apply] [Copy]     │
│ ─────────────────────────────────── │
│ [View Logs ▼] [Re-run Test]        │
└─────────────────────────────────────┘
```

### 3. 典型用户流程

#### 流程 1：Debug → Plan → Agent → 应用 diff → 复测
1. **Debug 模式**：检测到错误 → 显示诊断信息
2. **切换到 Plan**：基于诊断生成修复计划
3. **编辑 Plan**：用户确认/修改计划
4. **锁定 Plan**：点击 "Lock Plan"
5. **切换到 Agent**：自动加载 Plan，开始执行
6. **查看变更**：Agent 显示文件变更预览
7. **应用 diff**：用户点击 "Apply All"
8. **复测**：返回 Debug 模式，点击 "Re-run Test"

#### 流程 2：Chat → 添加上下文 → 生成代码 → 应用
1. **Chat 模式**：用户提问
2. **添加上下文**：选中代码 → 右键 "Add to Context"
3. **AI 响应**：生成代码片段
4. **查看代码块**：代码块显示工具条
5. **应用代码**：点击 "Insert" → 插入到编辑器光标位置

#### 流程 3：Plan → 执行 → 部分回滚
1. **Plan 模式**：创建开发计划
2. **切换到 Agent**：开始执行
3. **执行中**：某个步骤失败
4. **查看变更**：查看已完成的变更
5. **部分应用**：只应用成功的步骤
6. **回滚失败步骤**：点击 "Rollback" → 选择要回滚的文件

### 4. 三种风格方向

#### 方向 1：极简（Minimal）
**特点**：更少的视觉元素，更大的留白

**Token 调整：**
- 间距：`--space-*` 增加 20%（更宽松）
- 边框：`--border-subtle` 透明度降低到 0.02
- 圆角：减少 2px（更方正）
- 阴影：更轻微（透明度降低 20%）
- 字体：字号减少 1px（更精致）

**适用场景**：追求极致简洁的用户

#### 方向 2：科技感（Tech）
**特点**：更强烈的对比，更明显的层次

**Token 调整：**
- 背景对比：`--bg-0` 和 `--bg-1` 对比度增加
- 边框：`--border-default` 透明度增加到 0.1
- 强调色：使用更亮的蓝色（`#60a5fa`）
- 代码块：背景更深（`rgba(0, 0, 0, 0.4)`）
- 动效：时长减少到 120ms（更快速）

**适用场景**：喜欢强烈视觉反馈的用户

#### 方向 3：温和（Gentle）
**特点**：更柔和的色彩，更舒适的阅读

**Token 调整：**
- 背景：使用更暖的深色（`#0d0d0f` 替代 `#0a0a0c`）
- 文本：对比度降低 10%（更柔和）
- 强调色：使用更柔和的蓝色（`#4a9eff`）
- 圆角：增加 2px（更圆润）
- 动效：时长增加到 220ms（更缓慢）

**适用场景**：长时间使用，追求舒适度的用户

---

## 实现检查清单

- [ ] 设计 Tokens 已转换为 CSS 变量
- [ ] 四模式组件已实现基础结构
- [ ] 模式切换逻辑已实现
- [ ] 上下文管理已实现
- [ ] 与编辑器联动已实现
- [ ] 动效已应用
- [ ] 响应式布局已测试
- [ ] 无障碍访问已考虑（键盘导航、屏幕阅读器）

---

**文档版本**：v1.0  
**最后更新**：2025-01-25  
**维护者**：MindCode Design Team
